# =============================================================
# Pinned dependencies for the Heterogeneous Realtime Financial Data Platform.
#
# Target runtime  : Python 3.8 (FinStream lab server); tested on 3.11 (Docker).
# Airflow version : 2.8.1 (FinStream production pin).
# PySpark         : not used by this pipeline (Airflow + Postgres + Mongo only).
#
# Airflow itself is installed inside the container via the constraints file:
#   pip install "apache-airflow==2.8.1" \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.8.1/constraints-3.8.txt"
# Do NOT `pip install apache-airflow` from this requirements.txt directly.
# =============================================================

# --- Core data stores (used by every DAG and FastAPI) ---
psycopg2-binary==2.9.9
pymongo==4.6.3          # last 4.x line compatible with pip-installed pymongo on Py3.8

# --- HTTP / data sources ---
requests==2.31.0        # matches template's pin
aiohttp==3.9.5

# --- Airflow orchestration (installed via constraints file; listed for doc only) ---
# apache-airflow==2.8.1
pendulum==2.1.2         # Airflow 2.8.1 pinned dependency

# --- Entity mapping (L2) ---
spacy>=3.7,<3.8         # 3.7.x has best Py3.8 wheel coverage
# spaCy model — install separately:
#   python -m spacy download en_core_web_sm

# --- Sentiment scoring (L3) ---
transformers==4.36.2    # last 4.36 line compatible with torch 2.1 + Py3.8
torch==2.1.2            # CPU build; use:
                        #   pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==2.7.0

# --- FastAPI serving layer ---
fastapi==0.111.0
uvicorn[standard]==0.30.1

# --- Wikipedia S&P 500 scraping (sp500_refresh_dag) ---
pandas==2.0.3           # last 2.0.x line still on Python 3.8
lxml==5.2.2             # required by pandas.read_html
beautifulsoup4==4.12.3

# --- YAML config (template requirement) ---
pyyaml==6.0.1

# --- Developer / typing utilities ---
typer==0.12.3
typing-extensions==4.12.2
