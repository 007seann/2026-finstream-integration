# Pin versions compatible with our production stack (Python 3.8, Airflow 2.8.1, PySpark 3.5.1).
# Verify any new library's Python 3.8 compatibility before adding it here.
pyyaml==6.0.1
requests==2.31.0
